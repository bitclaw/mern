let contentNode = document.getElementById('contents');
let component = <h1>Hello World!</h1>;   // A simple JSX component
ReactDOM.render(component, contentNode);   // Render the component inside the content Node